#include<cstdio>
#include<vector>

// declare namespace (ask compiler to make exceptions)
using std::vector;
using std::printf;

// Function declarations
void print_queue(vector<int>);
vector<int> push_q(vector<int>, int);
int pop_q(vector<int>);