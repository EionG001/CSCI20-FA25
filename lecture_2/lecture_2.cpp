//cpp code

#include <cstdio>

int main() {
    int x = 1;
    bool b = true; // Boolean value
    std::printf("%i", b); // Booleans are numbers
    std::printf("%i", x);
    std::printf("hello world");
    return 0;
}